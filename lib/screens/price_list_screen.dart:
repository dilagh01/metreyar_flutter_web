import 'package:flutter/material.dart';

class PriceListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("صفحه لیست قیمت")),
    );
  }
}
