import 'package:flutter/material.dart';

class WorkflowScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("گردش کار")),
      body: const Center(
        child: Text("فرآیند گردش کار اینجا نشون داده میشه"),
      ),
    );
  }
}
